import { EsqueletoInforme } from "@/components/analysis/EsqueletoInforme";

// La forma del informe en barras, sin texto, sin pasos y sin tiempo (24-sep-2026): ver
// `EsqueletoInforme`. La misma para LTR, STR y AMBAS.
export default function AnalisisLoading() {
  return <EsqueletoInforme />;
}
